/* ADDITIVE ONLY — do not replace existing AF / legado rules.
   Paste this BLOCK inside the existing
   match /databases/{database}/documents { ... }
   without deleting anything that already works.

   Collections used by https://akashahub.com.br/academy
*/

    function isAcademyCeo() {
      return request.auth != null && (
        request.auth.uid == "1GO7dRdFUFg2NwYwzOwjvtWfpAS2"
        || request.auth.token.email in [
          "yanfili.simon@gmail.com",
          "plmacramo@gmail.com",
          "sendatantrica@gmail.com",
          "opatricksimon@gmail.com"
        ]
      );
    }

    match /academy_profiles/{uid} {
      allow read: if request.auth != null && (request.auth.uid == uid || isAcademyCeo());
      allow create, update: if request.auth != null && request.auth.uid == uid;
      allow delete: if isAcademyCeo();
    }

    match /academy_attendance/{id} {
      allow read: if request.auth != null && (
        isAcademyCeo()
        || resource.data.uid == request.auth.uid
      );
      allow create: if request.auth != null
        && request.resource.data.uid == request.auth.uid;
      allow update, delete: if isAcademyCeo();
    }
