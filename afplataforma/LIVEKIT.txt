LIVEKIT — ligar o servidor de mídia
===================================

A plataforma já pede token em:
https://akasha.yanfili-simon.workers.dev

Hoje esse Worker responde "Hello World!". Sem o token, a call cai no WebRTC antigo.

1. Cloudflare Dashboard → Workers → o worker akasha.yanfili-simon
2. Cole o código de functions/mint-livekit-token.js
3. Settings → Variables / Secrets:
   LIVEKIT_API_KEY
   LIVEKIT_API_SECRET
   LIVEKIT_WS_URL = wss://akashahub-vlya29kl.livekit.cloud
4. Save + Deploy
5. Teste: POST JSON { "identity":"teste", "room":"mentoria-principal", "name":"Yan" }
   Tem que voltar { token, url }

Chaves: LiveKit Cloud → projeto akashahub → Settings → Keys.
