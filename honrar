<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>HONRAR A DEUSA | Yan Simon — O Despertar Magnético</title>
    
    <!-- Fontes Premium (Elegância + Poder) -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,700;1,400&family=Tenor+Sans&display=swap" rel="stylesheet">

    <style>
        /* ========================================
           SISTEMA DE DESIGN — DEUSA
           ======================================== */
        :root {
            --goddess-rose: #FF69B4;
            --sacred-gold: #FFD700;
            --divine-purple: #9370DB;
            --void-black: #000000;
            --text-silk: #F5F5F5;
            --text-muted: #A0A0A0;
        }

        * { 
            margin: 0; 
            padding: 0; 
            box-sizing: border-box;
        }

        body, html {
            background: var(--void-black);
            color: var(--text-silk);
            font-family: 'Tenor Sans', sans-serif;
            overflow-x: hidden;
            -webkit-font-smoothing: antialiased;
        }

        /* ========================================
           CANVAS 3D — O PALCO SAGRADO
           ======================================== */
        #divine-canvas {
            position: fixed;
            top: 0; left: 0;
            width: 100%; height: 100%;
            z-index: -1;
        }

        /* ========================================
           TIPOGRAFIA CINEMATOGRÁFICA
           ======================================== */
        h1, h2, h3 {
            font-family: 'Playfair Display', serif;
            line-height: 1.1;
            margin: 0;
        }

        .hero-title {
            font-size: clamp(3rem, 10vw, 8rem);
            font-style: italic;
            font-weight: 400;
            text-align: center;
            background: linear-gradient(135deg, var(--goddess-rose), var(--sacred-gold));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            opacity: 0; /* Controlado via GSAP */
            letter-spacing: -0.02em;
        }

        .subtitle {
            font-size: clamp(0.9rem, 2.5vw, 1.2rem);
            text-align: center;
            color: var(--text-muted);
            text-transform: uppercase;
            letter-spacing: 0.3em;
            margin-top: 15px;
            font-weight: 300;
        }

        /* ========================================
           ESTRUTURA NARRATIVA (SCROLL SECTIONS)
           ======================================== */
        .narrative-flow {
            position: relative;
            z-index: 2;
        }

        section {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 60px 20px;
            text-align: center;
        }

        p {
            font-size: clamp(1.1rem, 2.5vw, 1.4rem);
            line-height: 1.9;
            max-width: 700px;
            color: var(--text-muted);
            margin-top: 30px;
        }

        .highlight {
            color: var(--goddess-rose);
            font-weight: 700;
            font-style: italic;
        }

        .quote {
            font-family: 'Playfair Display', serif;
            font-size: clamp(1.5rem, 4vw, 2.5rem);
            font-style: italic;
            line-height: 1.4;
            color: var(--text-silk);
            max-width: 800px;
            margin: 40px auto;
            position: relative;
            padding: 0 40px;
        }

        .quote::before {
            content: '"';
            position: absolute;
            left: 0; top: -20px;
            font-size: 5rem;
            color: var(--goddess-rose);
            opacity: 0.3;
        }

        /* ========================================
           LIVRO 3D ULTRA-REALISTA
           ======================================== */
        .book-showcase {
            perspective: 1500px;
            margin: 60px auto;
        }

        .book-3d {
            width: 280px;
            height: 400px;
            position: relative;
            transform-style: preserve-3d;
            transition: transform 1s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            opacity: 0; /* Animado */
        }

        .book-cover {
            width: 100%; height: 100%;
            background: linear-gradient(135deg, #1a1a1a, #2d2d2d);
            border: 3px solid var(--goddess-rose);
            border-radius: 8px;
            box-shadow: 
                0 40px 80px rgba(0,0,0,0.9),
                0 0 60px rgba(255, 105, 180, 0.3),
                inset 0 0 30px rgba(255, 105, 180, 0.1);
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            padding: 40px;
            position: relative;
            overflow: hidden;
        }

        /* Efeito de brilho deslizante */
        .book-cover::before {
            content: '';
            position: absolute;
            top: 0; left: -100%;
            width: 50%; height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
            animation: book-shine 4s infinite;
        }

        @keyframes book-shine {
            0% { left: -100%; }
            50% { left: 100%; }
            100% { left: 100%; }
        }

        .book-cover h3 {
            font-size: 2.2rem;
            color: var(--goddess-rose);
            text-shadow: 0 0 20px rgba(255, 105, 180, 0.5);
            margin-bottom: 15px;
        }

        .book-cover p {
            font-size: 0.9rem;
            color: var(--text-muted);
            margin: 0;
        }

        /* Lombada 3D */
        .book-spine {
            position: absolute;
            top: 10px; left: -20px;
            width: 20px; height: calc(100% - 20px);
            background: linear-gradient(to right, #0a0a0a, #1a1a1a);
            transform: rotateY(-15deg);
            border-radius: 4px 0 0 4px;
        }

        /* ========================================
           PREÇO & CTA
           ======================================== */
        .price-divine {
            font-family: 'Playfair Display', serif;
            font-size: clamp(3.5rem, 8vw, 5rem);
            color: var(--sacred-gold);
            text-shadow: 0 0 40px rgba(255, 215, 0, 0.5);
            margin: 30px 0;
            font-weight: 700;
        }

        .cta-floating {
            position: fixed;
            bottom: 40px;
            left: 50%;
            transform: translateX(-50%);
            z-index: 100;
        }

        .btn-goddess {
            background: linear-gradient(135deg, var(--goddess-rose), var(--divine-purple));
            color: white;
            border: none;
            padding: 22px 70px;
            font-family: 'Playfair Display', serif;
            font-size: 1.4rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 3px;
            border-radius: 4px;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
            box-shadow: 
                0 0 50px rgba(255, 105, 180, 0.4),
                0 20px 40px rgba(0,0,0,0.5);
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            animation: goddess-pulse 3s infinite;
        }

        @keyframes goddess-pulse {
            0%, 100% { 
                box-shadow: 
                    0 0 30px rgba(255, 105, 180, 0.3),
                    0 20px 40px rgba(0,0,0,0.5);
            }
            50% { 
                box-shadow: 
                    0 0 60px rgba(255, 105, 180, 0.7),
                    0 20px 60px rgba(255, 105, 180, 0.3);
            }
        }

        .btn-goddess:hover {
            transform: scale(1.08) translateY(-5px);
            box-shadow: 
                0 0 80px rgba(255, 105, 180, 0.8),
                0 30px 60px rgba(255, 105, 180, 0.4);
        }

        /* ========================================
           LOADING SCREEN
           ======================================== */
        .portal-loader {
            position: fixed;
            top: 0; left: 0;
            width: 100%; height: 100%;
            background: var(--void-black);
            z-index: 1000;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            transition: opacity 1.5s ease;
        }

        .loader-text {
            font-family: 'Playfair Display', serif;
            font-size: 1.8rem;
            font-style: italic;
            color: var(--goddess-rose);
            margin-top: 30px;
            letter-spacing: 0.2em;
        }

        .loader-symbol {
            width: 80px; height: 80px;
            border: 3px solid transparent;
            border-top-color: var(--goddess-rose);
            border-radius: 50%;
            animation: spin 1.5s linear infinite;
        }

        @keyframes spin {
            100% { transform: rotate(360deg); }
        }

        /* ========================================
           RESPONSIVIDADE MOBILE
           ======================================== */
        @media (max-width: 768px) {
            .btn-goddess {
                padding: 18px 40px;
                font-size: 1.1rem;
                letter-spacing: 2px;
            }

            .cta-floating {
                bottom: 20px;
                width: 90%;
            }

            .book-3d {
                width: 220px;
                height: 320px;
            }
        }
    </style>
</head>
<body>

    <!-- LOADING PORTAL -->
    <div class="portal-loader" id="loader">
        <div class="loader-symbol"></div>
        <div class="loader-text">Invocando a Deusa...</div>
    </div>

    <!-- CANVAS 3D -->
    <div id="divine-canvas"></div>

    <!-- CTA FLUTUANTE -->
    <div class="cta-floating">
        <a href="https://pay.hotmart.com/M104187005M?sck=HOTMART_PRODUCT_PAGE&off=1yhzjgaz&hotfeature=32" 
           class="btn-goddess" 
           target="_blank">
            DESPERTAR AGORA
        </a>
    </div>

    <!-- NARRATIVA EM SCROLL -->
    <div class="narrative-flow">
        
        <!-- ATO I: ABERTURA -->
        <section id="act1">
            <h1 class="hero-title">Honrar a Deusa</h1>
            <p class="subtitle">A Maestria Que Não Se Ensina. Se Desperta.</p>
        </section>

        <!-- ATO II: A REVELAÇÃO -->
        <section id="act2">
            <p class="quote">
                Seduzir não é uma técnica.<br>
                É uma <span class="highlight">consequência</span> de honrar o sagrado feminino.
            </p>
            <p>
                Você não aprende truques. Você se torna o tipo de homem que inspira 
                <strong>confiança, desejo e admiração</strong> apenas por existir.
            </p>
        </section>

        <!-- ATO III: A TRANSFORMAÇÃO -->
        <section id="act3">
            <h2 style="font-size: clamp(2rem, 5vw, 3.5rem); color: var(--goddess-rose); margin-bottom: 30px;">
                O Despertar Magnético
            </h2>
            <p>
                Yan Simon revela o caminho da verdadeira maestria relacional:<br>
                Um <span class="highlight">novo estado de ser</span> onde o sucesso amoroso, 
                sexual e pessoal acontece naturalmente.
            </p>
            <p style="margin-top: 40px; font-style: italic; color: var(--text-silk);">
                "Você não se entende melhor.<br>
                <strong style="color: var(--sacred-gold);">Você se torna inevitável.</strong>"
            </p>
        </section>

        <!-- ATO IV: AUTORIDADE -->
        <section id="act4">
            <h2 style="font-size: 2.5rem; color: var(--divine-purple);">Yan Simon</h2>
            <p>
                30+ modalidades terapêuticas avançadas.<br>
                Imerso no Tantra autêntico desde 2020.<br><br>
                Ele não explica teorias. <strong>Ele ativa estados superiores.</strong><br>
                Centenas de vidas transformadas. Resultados radicais.
            </p>
        </section>

        <!-- ATO V: O ARTEFATO -->
        <section id="act5">
            <div class="book-showcase">
                <div class="book-3d">
                    <div class="book-spine"></div>
                    <div class="book-cover">
                        <h3>HONRAR<br>A DEUSA</h3>
                        <p>Yan Simon</p>
                    </div>
                </div>
            </div>

            <h2 style="font-size: clamp(1.8rem, 4vw, 2.5rem); margin-top: 40px;">
                Domine a Energia da Vida.<br>
                Reconecte-se ao Sagrado Feminino.
            </h2>

            <div class="price-divine">R$ 47,71</div>

            <p style="font-size: 1rem; opacity: 0.6; margin-top: 20px;">
                Acesso Imediato | Pagamento Seguro Hotmart
            </p>
        </section>

    </div>

    <!-- ========================================
         ENGINE 3D — NÍVEL AAA
         ======================================== -->
    <script type="module">
        import * as THREE from 'https://unpkg.com/three@0.126.1/build/three.module.js';
        import { EffectComposer } from 'https://unpkg.com/three@0.126.1/examples/jsm/postprocessing/EffectComposer.js';
        import { RenderPass } from 'https://unpkg.com/three@0.126.1/examples/jsm/postprocessing/RenderPass.js';
        import { UnrealBloomPass } from 'https://unpkg.com/three@0.126.1/examples/jsm/postprocessing/UnrealBloomPass.js';
        import { gsap } from 'https://unpkg.com/gsap@3.12.2/index.js';
        import { ScrollTrigger } from 'https://unpkg.com/gsap@3.12.2/ScrollTrigger.js';

        gsap.registerPlugin(ScrollTrigger);

        // ==========================================
        // 1. SETUP DA CENA SAGRADA
        // ==========================================
        const container = document.getElementById('divine-canvas');
        const scene = new THREE.Scene();
        scene.fog = new THREE.FogExp2(0x000000, 0.01);

        const camera = new THREE.PerspectiveCamera(
            75, 
            window.innerWidth / window.innerHeight, 
            0.1, 
            100
        );
        camera.position.z = 10;

        const renderer = new THREE.WebGLRenderer({ 
            antialias: true, 
            alpha: true,
            powerPreference: "high-performance"
        });
        renderer.setSize(window.innerWidth, window.innerHeight);
        renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
        renderer.toneMapping = THREE.ReinhardToneMapping;
        renderer.toneMappingExposure = 1.2;
        container.appendChild(renderer.domElement);

        // POST-PROCESSING (TRIPLE LAYER)
        const renderScene = new RenderPass(scene, camera);
        
        const bloomPass = new UnrealBloomPass(
            new THREE.Vector2(window.innerWidth, window.innerHeight),
            2.0,  // Strength (brilho intenso)
            0.5,  // Radius
            0.85  // Threshold
        );

        const composer = new EffectComposer(renderer);
        composer.addPass(renderScene);
        composer.addPass(bloomPass);

        // ==========================================
        // 2. A DEUSA — GEOMETRIA SAGRADA
        // ==========================================
        
        // GRUPO PRINCIPAL (Para rotação 360°)
        const goddessGroup = new THREE.Group();
        scene.add(goddessGroup);

        // NÚCLEO: Flor da Vida (Simplificada com TorusKnot)
        const coreGeo = new THREE.TorusKnotGeometry(2, 0.6, 150, 20, 2, 3);
        const coreMat = new THREE.MeshBasicMaterial({
            color: 0xFF69B4, // Rosa Deusa
            wireframe: true,
            transparent: true,
            opacity: 0.3
        });
        const core = new THREE.Mesh(coreGeo, coreMat);
        goddessGroup.add(core);

        // AURA EXTERNA: Partículas Femininas (Rosa)
        const particlesCount = 2000;
        const particlesGeo = new THREE.BufferGeometry();
        const posArray = new Float32Array(particlesCount * 3);

        for(let i=0; i<particlesCount; i++) {
            // Distribuição esférica
            const theta = Math.random() * Math.PI * 2;
            const phi = Math.acos(2 * Math.random() - 1);
            const radius = 3 + Math.random() * 2;

            posArray[i*3] = radius * Math.sin(phi) * Math.cos(theta);
            posArray[i*3+1] = radius * Math.sin(phi) * Math.sin(theta);
            posArray[i*3+2] = radius * Math.cos(phi);
        }

        particlesGeo.setAttribute('position', new THREE.BufferAttribute(posArray, 3));
        
        const particlesMat = new THREE.PointsMaterial({
            size: 0.05,
            color: 0xFF69B4,
            transparent: true,
            opacity: 0.6,
            blending: THREE.AdditiveBlending
        });

        const aura = new THREE.Points(particlesGeo, particlesMat);
        goddessGroup.add(aura);

        // CAMADA SECUNDÁRIA: Partículas Masculinas (Azul claro)
        const particlesGeo2 = new THREE.BufferGeometry();
        const posArray2 = new Float32Array(particlesCount * 3);

        for(let i=0; i<particlesCount; i++) {
            posArray2[i*3] = (Math.random() - 0.5) * 10;
            posArray2[i*3+1] = (Math.random() - 0.5) * 10;
            posArray2[i*3+2] = (Math.random() - 0.5) * 10;
        }

        particlesGeo2.setAttribute('position', new THREE.BufferAttribute(posArray2, 3));
        
        const particlesMat2 = new THREE.PointsMaterial({
            size: 0.03,
            color: 0x87CEEB, // Azul céu (masculino)
            transparent: true,
            opacity: 0.4,
            blending: THREE.AdditiveBlending
        });

        const masculine = new THREE.Points(particlesGeo2, particlesMat2);
        scene.add(masculine);

        // ==========================================
        // 3. NARRATIVA VISUAL (GSAP SCROLL)
        // ==========================================

        // Entrada do Título
        gsap.to(".hero-title", { 
            opacity: 1, 
            scale: 1, 
            duration: 3, 
            ease: "power4.out",
            delay: 0.5
        });

        // TIMELINE MESTRA
        const masterTimeline = gsap.timeline({
            scrollTrigger: {
                trigger: ".narrative-flow",
                start: "top top",
                end: "bottom bottom",
                scrub: 0.5 // Precisão cirúrgica
            }
        });

        // ATO I -> II: Invocação (Deusa se materializa)
        masterTimeline
            .to(goddessGroup.scale, { 
                x: 1.5, y: 1.5, z: 1.5, 
                duration: 4 
            }, 0)
            .to(coreMat, { 
                opacity: 0.8, 
                duration: 3 
            }, 0)
            .to(particlesMat, { 
                opacity: 1, 
                size: 0.08,
                duration: 3 
            }, 0);

        // ATO II -> III: Dança (Rotação 360°)
        masterTimeline
            .to(goddessGroup.rotation, { 
                y: Math.PI * 2, 
                x: Math.PI, 
                duration: 6 
            }, 4)
            .to(core.rotation, { 
                z: Math.PI * 4, 
                duration: 6 
            }, 4);

        // ATO III -> IV: União Alquímica (Partículas se fundem)
        masterTimeline
            .to(particlesMat, { 
                r: 1, g: 0.84, b: 0, // Ouro (RGB de #FFD700)
                duration: 3 
            }, 10)
            .to(particlesMat2, { 
                r: 1, g: 0.84, b: 0,
                opacity: 0.8,
                size: 0.06,
                duration: 3 
            }, 10)
            .to(masculine.position, { 
                x: 0, y: 0, z: 0, // Convergem para o centro
                duration: 4 
            }, 10);

        // ATO IV -> V: Explosão de Luz (Revelação do Livro)
        masterTimeline
            .to(goddessGroup.scale, { 
                x: 3, y: 3, z: 3, 
                duration: 3 
            }, 14)
            .to(coreMat, { 
                opacity: 0, 
                duration: 2 
            }, 15)
            .to(camera.position, { 
                z: 6, 
                duration: 3 
            }, 14)
            .to(goddessGroup.position, { 
                x: -5, // Sai de cena à esquerda
                duration: 3 
            }, 16);

        // Animações de Texto (Cascata)
        gsap.utils.toArray("section").forEach((section, i) => {
            if(i === 0) return; // Pula hero
            
            gsap.from(section.children, {
                y: 80,
                opacity: 0,
                duration: 1.5,
                stagger: 0.2,
                ease: "power3.out",
                scrollTrigger: {
                    trigger: section,
                    start: "top 70%",
                    toggleActions: "play none none reverse"
                }
            });
        });

        // Revelar Livro 3D com Rotação
        gsap.to(".book-3d", {
            opacity: 1,
            rotateY: 360,
            duration: 2.5,
            ease: "power2.out",
            scrollTrigger: {
                trigger: "#act5",
                start: "top 60%"
            }
        });

        // ==========================================
        // 4. LOOP DE RENDERIZAÇÃO (60 FPS)
        // ==========================================
        const clock = new THREE.Clock();

        // Mouse Parallax Avançado
        let mouseX = 0;
        let mouseY = 0;
        let targetX = 0;
        let targetY = 0;

        window.addEventListener('mousemove', (e) => {
            mouseX = (e.clientX - window.innerWidth/2) * 0.0008;
            mouseY = (e.clientY - window.innerHeight/2) * 0.0008;
        });

        function animate() {
            const elapsedTime = clock.getElapsedTime();

            // Rotação contínua suave (Vida própria)
            goddessGroup.rotation.y += 0.001;
            core.rotation.z -= 0.003;

            // Pulsação do núcleo (Respiração)
            const pulse = Math.sin(elapsedTime * 1.2) * 0.15 + 1;
            core.scale.set(pulse, pulse, pulse);

            // Partículas orbitando (Feminino)
            aura.rotation.y += 0.0005;
            aura.rotation.x = Math.sin(elapsedTime * 0.3) * 0.2;

            // Parallax Suave (Inércia)
            targetX = mouseX * 3;
            targetY = mouseY * 3;
            
            goddessGroup.rotation.y += 0.05 * (targetX - goddessGroup.rotation.y);
            goddessGroup.rotation.x += 0.05 * (targetY - goddessGroup.rotation.x);

            // Render com Post-Processing
            composer.render();
            requestAnimationFrame(animate);
        }

        // ==========================================
        // 5. RESPONSIVIDADE & INIT
        // ==========================================
        window.addEventListener('resize', () => {
            camera.aspect = window.innerWidth / window.innerHeight;
            camera.updateProjectionMatrix();
            renderer.setSize(window.innerWidth, window.innerHeight);
            composer.setSize(window.innerWidth, window.innerHeight);

            // Ajuste de câmera para mobile
            if(window.innerWidth < 768) {
                camera.position.z = 12;
            } else {
                camera.position.z = 10;
            }
        });

        // Inicialização
        window.addEventListener('load', () => {
            const loader = document.getElementById('loader');
            loader.style.opacity = '0';
            
            setTimeout(() => {
                loader.remove();
                animate();
            }, 1500);
        });

    </script>
</body>
</html>
